server:
  port: 8020

spring:
  application:
    name: nacos-config
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yml
  # 此项目中dataId: nacos-config.yml
  # ${prefix}: nacos-config, ${spring.profiles.active}: 空, ${file-extension}: yml
